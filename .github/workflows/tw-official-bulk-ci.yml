name: TW Official Credential Marker Diagnostic

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: b22cf60e8cee4f416aef3ddfc701b59cbd09980e
          fetch-depth: 0
      - name: Report marker matches without values
        shell: bash
        run: |
          python3 - <<'PY'
          import pathlib
          import subprocess

          needles = {
              'private_key_marker': '-----BEGIN PRIVATE KEY-----',
              'supabase_secret_prefix': 'sb_secret_',
              'google_api_prefix': 'AIza',
              'bearer_example': 'Authorization: Bearer ',
          }
          files = subprocess.check_output(
              ['git','diff','--name-only','origin/main...HEAD'], text=True
          ).splitlines()
          found = []
          for name in files:
              path = pathlib.Path(name)
              if not path.is_file():
                  continue
              text = path.read_text(encoding='utf-8')
              for line_no, line in enumerate(text.splitlines(), 1):
                  for category, needle in needles.items():
                      if needle in line:
                          found.append((name, line_no, category))
          for name, line_no, category in found:
              print(f'{name}:{line_no}:{category}')
          if not found:
              print('no marker matches')
          PY
